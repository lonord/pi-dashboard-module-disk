import Main from './main'

// tslint:disable-next-line:no-var-requires
const pkg = require('../package.json')

export const size = 'normal'
export const Comp = Main
export const version = pkg.version
export const name = '路由器磁盘'
